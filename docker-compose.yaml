services:
  web:
    build: .
    ports:
      - '80:3000'
    environment:
      ADMIN_USER: admin
      ADMIN_PASS: admin123
    volumes:
      - projects_data:/app/data
    restart: always

volumes:
  projects_data:
